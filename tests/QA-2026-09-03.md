# Dashboard test and debug review — 2026-09-03

Baseline: `b1dea45`. Scope: recent STOCK CARD, BOM spacing and prefix-5 exclusion changes; regression tests; stock, BOM and pallet UI smoke tests; mobile/desktop layout; deployment readiness.

## Reproduced and fixed

1. Opening KOTA BOM `21-0021-03` on a 390×844 viewport expanded the document from 375 to 753 CSS pixels. The BOM wrapper's inline `overflow:visible` overrode its scrolling class. Removed the override and gave the scroll region a keyboard focus target and accessible name. Retest: document 375px, wrapper 323px, scrollable table 727px.
2. Manually excluding carton `31-0021-03-6201` increased calculated production from 431 to 2,522, but the excluded row still deducted stock, showing -29.056 pcs / -872 baht. Excluded rows now retain their original stock and value (6 pcs / 180 baht in the isolated fixture). Restoring the row returns production to 431. Excluding every row does not imply production readiness.

## Automated verification

`node --test tests/*.test.cjs`: all 15 suites pass. New `bom-render.test.cjs` checks exclusion/restoration, retained values, all-excluded behavior and the scroll region. Existing coverage checks prefix-5 removal across all embedded formulas, empty formulas, stock refresh, BOM search, movement validation and persistence errors, concurrent writes, dates, FIFO, pagination, map geometry, zones and unchanged embedded data/assets. All three inline scripts parse; `git diff --check` passes.

## Browser verification

Used `tests/preview-history.cjs` at localhost:8767 with Supabase URLs disabled and synthetic inventory.

- 390×844: Stock/BOM pages fit the document width; BOM table scrolls within its wrapper. Warehouse filter selected Hold and updated the summary.
- 1440×900: dark-mode BOM summary renders without document overflow. Theme toggle succeeds.
- BOM lookup, prefix-5 removal, manual exclude/restore and corresponding balances verified.
- FIFO result order: C-05 (01/08), D-21 (04/08), C-06 (04/08), D-16 (01/09).
- STOCK CARD receipt edited from 21 to 22 after a simulated remote refresh; draft retained and ending balance updated from 14 to 15.
- Withdrawal 999 rejected with a negative-balance error.
- Simulated save failure at desktop size retained draft 24, displayed the error and allowed retry. A subsequent conflicting refresh retained the draft and disabled save.
- Pallet and overview rotation controls operated; zone dialog opened and closed.
- No error/warning browser logs during the tested local flows. The simulated network failure appeared as an expected form alert.

## Deployment gate and limits

Deploy the tested changes to the existing GitHub Pages main branch; verify the matching Actions deployment and live BOM region afterwards. Roll back the release commit if page initialization, BOM totals or table access regress. Rollback baseline: `b1dea45`. No database migration is required.

Production inventory writes were not exercised; save failures/concurrency were simulated locally. This review covers tested frontend flows, not server-side RLS, all database permissions or every legacy write path. Embedded inventory/BOM/maps remain unchanged. No claim of exhaustive security coverage is made.
